# FIG. 3 parts inventory — MyUI

Captured 2026-07-28 from checked-in portfolio authority and case-study copy.
Every visible part below has a stable repository anchor. **Nothing here is
inferred.**

This file exists so the plate has an evidence base: a labeled or outlined part
may only appear when the source states it and the employer boundary permits it.

## Parts

| Part | Evidence |
| --- | --- |
| Ellucian Experience shell | `docs/REDESIGN_PLAN.md`, “Specs for the three remaining index plates,” FIG. 3: “inside the Ellucian Experience shell”; `src/app/projects/myui/page.tsx`, metadata: “built on Ellucian Experience platform” |
| Collective custom React component layer | `docs/REDESIGN_PLAN.md`, same FIG. 3 spec: “custom React components”; `src/app/projects/myui/page.tsx`, Introduction: “creating custom React components … that integrate … with the Ellucian Experience platform” |
| Component layer contained by the shell | `docs/REDESIGN_PLAN.md`, same FIG. 3 spec: “components inside a platform the university does not own, not an application built from nothing” |

## Deliberately not drawn

- No student-facing data, dashboard content, or record-like details. PRODUCT.md
  places MyUI behind the University of Idaho employer boundary, and the FIG. 3
  spec prohibits all three explicitly.
- No named component, service, API, data flow, platform feature, or screenshot.
  The plate states containment and platform ownership only.
- No individual component boxes or count. The evidence substantiates a
  collective component layer, not its cardinality, and separate boxes could be
  mistaken for dashboard cards.
- No University of Idaho ownership mark around the Ellucian shell. The
  structural fact is specifically that the university does not own the host
  platform.
- No arrows. The brief substantiates placement, not a flow or sequence.
